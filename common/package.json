{
  "name": "common",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "./dist/board.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf ./dist && tsc -b",
    "build:watch": "rm -rf ./dist && tsc -b --watch"
  },
  "author": "Richard Wotzlaw",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.1",
    "typescript": "^5.3.2"
  }
}
